import React from 'react';
// import PropTypes from 'prop-types';
import './style.scss';

export default class Publish extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>This is a publish page</div>
    );
  }
}

Publish.propTypes = {
};
